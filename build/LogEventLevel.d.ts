/**
 * The log event level
 *
 * @export
 * @enum {number}
 */
export declare enum LogEventLevel {
    off = 0,
    fatal = 1,
    error = 3,
    warning = 7,
    info = 15,
    debug = 31,
    verbose = 63
}
